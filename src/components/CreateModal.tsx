interface Props {}

export default function CreateModal({  }: Props) {
    return (
        <div className='h-full w-full flex items-center justify-center dark:bg-gray-800/50'>
            <div>Create Modal</div>
        </div>
    );
}